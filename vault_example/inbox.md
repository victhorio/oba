
- Check if `termflow` should support a `tf log --since <date>` helper for daily summaries.
- Draft a README section on caching strategy without overwhelming new users.
- Look up quick/slow mode prompts to see if they actually change latency.
- Decide whether to open-source immediately or wait until after a week of polish.
